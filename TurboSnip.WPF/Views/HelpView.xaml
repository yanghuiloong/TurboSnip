<UserControl x:Class="TurboSnip.WPF.Views.HelpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:p="clr-namespace:TurboSnip.WPF.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Background>
        <SolidColorBrush Color="Black" Opacity="0.8"/>
    </UserControl.Background>
    
    <Grid>
        <Border Background="#1E1E1E" CornerRadius="8" Margin="40"
                BorderBrush="#333" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.5"/>
            </Border.Effect>
            <Grid Margin="30">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="{Binding HelpTitle}" FontSize="32" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <StackPanel.Resources>
                            <Style TargetType="TextBlock">
                                <Setter Property="Margin" Value="0,0,0,15"/>
                                <Setter Property="FontSize" Value="16"/>
                            </Style>
                        </StackPanel.Resources>
                        <TextBlock Text="{Binding HelpFeaturesTitle}" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}"/>
                        <TextBlock Foreground="#DDD" TextWrapping="Wrap" LineHeight="24">
                            <Run Text="{Binding HelpPrivacyRaw}"/><LineBreak/>
                            <Run Text="{Binding HelpLiveCaptureRaw}"/><LineBreak/>
                            <Run Text="{Binding HelpPerformanceRaw}"/>
                        </TextBlock>

                        <TextBlock Text="{Binding HelpUsageTitle}" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}" Margin="0,15,0,0"/>
                        <TextBlock Foreground="#DDD" TextWrapping="Wrap" LineHeight="24">
                            <Run Text="{Binding HelpStep1Raw}"/><LineBreak/>
                            <Run Text="{Binding HelpStep2Raw}"/><LineBreak/>
                            <Run Text="{Binding HelpStep3Raw}"/><LineBreak/>
                            <Run Text="{Binding HelpStep4Raw}"/>
                        </TextBlock>
                    </StackPanel>
                </ScrollViewer>

                <Button Grid.Row="2" Content="{Binding HelpGotIt}" Height="40" Width="140" Margin="0,20,0,0"
                        Style="{StaticResource PrimaryButtonStyle}" 
                        Command="{Binding CloseHelpCommand}" FontSize="16"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>


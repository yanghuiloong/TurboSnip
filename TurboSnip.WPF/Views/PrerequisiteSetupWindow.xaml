<Window x:Class="TurboSnip.WPF.Views.PrerequisiteSetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TurboSnip - 环境配置" 
        Height="480" Width="550"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1E1E1E"
        WindowStyle="None"
        AllowsTransparency="True">
    
    <Border BorderBrush="#3D3D3D" BorderThickness="1" CornerRadius="8">
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Title -->
            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="🚀 TurboSnip 环境配置" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           Foreground="White"/>
                <TextBlock Text="首次运行需要安装必要的运行时组件" 
                           FontSize="13" 
                           Foreground="#808080"
                           Margin="0,5,0,0"/>
            </StackPanel>
            
            <!-- Status Cards -->
            <StackPanel Grid.Row="1" Margin="0,0,0,15">
                <!-- GPU Info -->
                <Border Background="#2D2D2D" CornerRadius="6" Padding="15" Margin="0,0,0,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="🖥️" FontSize="20" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="显卡" Foreground="#808080" FontSize="12"/>
                            <TextBlock x:Name="GpuNameText" Text="检测中..." Foreground="White" FontSize="14"/>
                        </StackPanel>
                        <TextBlock x:Name="GpuStatusIcon" Grid.Column="2" Text="⏳" FontSize="18" VerticalAlignment="Center"/>
                    </Grid>
                </Border>
                
                <!-- VC++ Runtime -->
                <Border Background="#2D2D2D" CornerRadius="6" Padding="15" Margin="0,0,0,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="📦" FontSize="20" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Visual C++ 运行库 (2015-2022)" Foreground="#808080" FontSize="12"/>
                            <TextBlock x:Name="VcRedistStatusText" Text="检测中..." Foreground="White" FontSize="14"/>
                        </StackPanel>
                        <TextBlock x:Name="VcRedistStatusIcon" Grid.Column="2" Text="⏳" FontSize="18" VerticalAlignment="Center"/>
                    </Grid>
                </Border>
                
                <!-- CUDA Runtime -->
                <Border Background="#2D2D2D" CornerRadius="6" Padding="15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="⚡" FontSize="20" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="CUDA 运行时 (GPU加速)" Foreground="#808080" FontSize="12"/>
                            <TextBlock x:Name="CudaStatusText" Text="检测中..." Foreground="White" FontSize="14"/>
                        </StackPanel>
                        <TextBlock x:Name="CudaStatusIcon" Grid.Column="2" Text="⏳" FontSize="18" VerticalAlignment="Center"/>
                    </Grid>
                </Border>
            </StackPanel>
            
            <!-- Progress Area - Hidden by default, shown when needed -->
            <Border Grid.Row="2" x:Name="ProgressPanel" Background="#252525" CornerRadius="6" Padding="15" Margin="0,0,0,15" Visibility="Collapsed">
                <StackPanel>
                    <TextBlock x:Name="ProgressTitle" 
                               Text="准备就绪" 
                               Foreground="White" 
                               FontSize="14" 
                               FontWeight="SemiBold"
                               Margin="0,0,0,10"/>
                    
                    <!-- Download Progress -->
                    <StackPanel x:Name="DownloadPanel" Visibility="Collapsed" Margin="0,0,0,10">
                        <TextBlock x:Name="DownloadText" 
                                   Text="正在下载..." 
                                   Foreground="#B0B0B0" 
                                   FontSize="12"
                                   Margin="0,0,0,5"/>
                        <ProgressBar x:Name="DownloadProgressBar" 
                                     Height="6" 
                                     Minimum="0" 
                                     Maximum="100" 
                                     Value="0"
                                     Background="#333333"
                                     Foreground="#4FC3F7"/>
                    </StackPanel>
                    
                    <!-- Installing Spinner -->
                    <StackPanel x:Name="InstallingPanel" Visibility="Collapsed" Orientation="Horizontal">
                        <Border x:Name="SpinnerBorder" Width="20" Height="20" Margin="0,0,10,0">
                            <Border.RenderTransform>
                                <RotateTransform x:Name="SpinnerRotation" CenterX="10" CenterY="10"/>
                            </Border.RenderTransform>
                            <Ellipse Stroke="#4FC3F7" StrokeThickness="3" StrokeDashArray="3,2"/>
                        </Border>
                        <TextBlock x:Name="InstallingText" 
                                   Text="正在安装，请稍候..." 
                                   Foreground="#B0B0B0" 
                                   FontSize="12"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <!-- Status Message -->
                    <TextBlock x:Name="StatusMessage" 
                               Text="" 
                               Foreground="#B0B0B0" 
                               FontSize="12"
                               TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
            
            <!-- Warning Message -->
            <Border Grid.Row="3" 
                    x:Name="WarningPanel"
                    Background="#3D2D00" 
                    CornerRadius="6" 
                    Padding="12" 
                    Margin="0,0,0,15"
                    Visibility="Collapsed">
                <TextBlock Text="⚠️ 必须安装所有必需组件才能使用 TurboSnip" 
                           Foreground="#FFB74D" 
                           FontSize="12"
                           TextWrapping="Wrap"/>
            </Border>
            
            <!-- Buttons -->
            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="ExitButton" 
                        Content="退出" 
                        Width="100" 
                        Height="38"
                        Margin="0,0,10,0"
                        Background="#424242"
                        Foreground="White"
                        BorderThickness="0"
                        Click="ExitButton_Click"/>
                <Button x:Name="InstallButton" 
                        Content="安装必需组件" 
                        Width="140" 
                        Height="38"
                        Background="#4CAF50"
                        Foreground="White"
                        FontWeight="SemiBold"
                        BorderThickness="0"
                        Click="InstallButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>

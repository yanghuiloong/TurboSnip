<Window x:Class="TurboSnip.WPF.Views.GpuWarmupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TurboSnip - 模型加载" 
        Height="260" Width="480"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1A1A2E"
        WindowStyle="None"
        AllowsTransparency="True"
        ShowInTaskbar="True">
    
    <Border BorderBrush="#16213E" BorderThickness="2" CornerRadius="12">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.5" Color="#0F3460"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Title Bar with Minimize Button -->
            <Grid Grid.Row="0" Background="#16213E" Height="32">
                <TextBlock Text="🚀 TurboSnip - 模型加载" 
                           Foreground="#E94560" 
                           FontSize="13" 
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           Margin="12,0,0,0"/>
                
                <!-- Minimize Button -->
                <Button x:Name="MinimizeButton"
                        Content="─"
                        HorizontalAlignment="Right"
                        Width="40" Height="32"
                        Background="Transparent"
                        Foreground="#808080"
                        BorderThickness="0"
                        FontSize="14"
                        FontWeight="Bold"
                        Click="MinimizeButton_Click"
                        ToolTip="最小化到任务栏">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="0,10,0,0">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#2D2D4A"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
            
            <!-- Content -->
            <Grid Grid.Row="1" Margin="35,20,35,25">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Status Area -->
                <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <!-- Spinner + Status -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Border x:Name="SpinnerBorder" Width="24" Height="24" Margin="0,0,12,0">
                            <Border.RenderTransform>
                                <RotateTransform x:Name="SpinnerRotation" CenterX="12" CenterY="12"/>
                            </Border.RenderTransform>
                            <Ellipse Stroke="#E94560" StrokeThickness="3" StrokeDashArray="2,1.5"/>
                        </Border>
                        <TextBlock x:Name="StatusText" 
                                   Text="正在加载模型..." 
                                   Foreground="White" 
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <!-- Model Name -->
                    <TextBlock x:Name="ModelNameText" 
                               Text="" 
                               Foreground="#7B8794" 
                               FontSize="13"
                               HorizontalAlignment="Center"
                               Margin="0,10,0,0"/>
                </StackPanel>
                
                <!-- Progress Bar -->
                <Border Grid.Row="1" Background="#16213E" CornerRadius="3" Height="6" Margin="0,10,0,0">
                    <ProgressBar x:Name="ProgressBar" 
                                 Height="6" 
                                 Minimum="0" 
                                 Maximum="100" 
                                 Value="0"
                                 Background="Transparent"
                                 Foreground="#E94560"
                                 BorderThickness="0"
                                 IsIndeterminate="True"/>
                </Border>
                
                <!-- Hint -->
                <TextBlock Grid.Row="2"
                           x:Name="HintText" 
                           Text="⏱️ 加载时间取决于显卡性能 | 可最小化窗口继续其他工作" 
                           Foreground="#5C6370" 
                           FontSize="11"
                           HorizontalAlignment="Center"
                           TextWrapping="Wrap"
                           TextAlignment="Center"
                           Margin="0,12,0,0"/>
            </Grid>
        </Grid>
    </Border>
</Window>
